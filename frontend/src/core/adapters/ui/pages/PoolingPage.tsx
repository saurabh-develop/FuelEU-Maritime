export default function PoolingPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold mb-3">Pooling</h2>
      <p>Work in progress — will connect to /pools API endpoints.</p>
    </div>
  );
}
